[read-doc-anydoc] 2026-08-24 小心：read 工具已移除办公/PDF 文档抽取（.docx/.pptx/.xlsx/.pdf 返回 E_DOCUMENT_UNSUPPORTED 并指路 anydoc skill）；不要再给 read 加文档解析或 sheet/maxChars 参数，读文档的唯一路径是 anydoc 转 .md 后再 read。危害：重新引入会与 schema 承诺冲突、模型行为不可预期。@internal/app/orch_read.go @internal/tools/shared/builtins.go
[win-explorer] 2026-08-22 小心：Go exec.Command 只在参数含空格时加引号，explorer.exe 收到无空格但含 = 等特殊字符的路径会解析失败并静默回退到“此电脑”。危害：文件浏览器“打开目录”对 D:\doc\=项目文档= 这类路径开错位置且不报错。修法：SysProcAttr.CmdLine 整段构造命令行并强制引号（explorerCommand）。@internal/app/host_filemanager_windows.go
[tool-kind-downstream] 2026-09-04 小心：工具分类异常时严禁在下游消费处硬加 !== 'specific_tool' 负向排除打补丁，必须回溯 toolKind 源头独立分类。危害：破坏单一真实源、到处硬编码特判劣化代码复杂度。@frontend/src/App.vue
[vue-ws-condense] 2026-09-09 小心：Vue SFC 模板里插值前后的纯空格文本节点会被 whitespace: 'condense' 移除（元素/插值边界处的空格不保留），不要靠模板里写空格做分隔。修法：把分隔符写进 JS 表达式（`{{ ' ' + x }}` 或 `{{ x + ' ' }}`）或用 CSS gap。危害：两个动态片段直接粘在一起，如 "1m 23s14:32"。@frontend/src/components/ChatMessages.vue
[tab-workspace] 2026-09-10 小心：per-Tab 的“当前工作区”必须由 Tab 自身解析（tabOwnsWorkspace / runWorkspaceForTab / activeRunWorkspace），任何仍拿 config.workspace 当参照的校验都会对 KB/临时 Tab 静默失效。危害：页脚上下文百分比常驻加载占位、git 徽标显示别的仓库、tokens:update 被丢弃，且全程不报错。@frontend/src/App.vue
[reasoning-replay] 2026-09-15 小心：思考模型的回传字段是**逐条助手消息**校验的（DeepSeek 明确要求带 tools 的请求里 reasoning_content 在后续每个请求完整回传，含没有工具调用的轮次），不是只校验尾部工具轮；而磁盘 profile 不存任何思考文本，所以恢复会话后必须由 transport 补一个显式空串——go-openai 的 `json:"reasoning_content,omitempty"` 会丢空串键，只能请求侧改写（chatRequestRewriteTransport，判据是端点而非模型名单：除 OpenAI 官方端点外一律补，**唯一例外是“关闭思考”档位**——请求本身就是在要求对方别思考，本轮没有任何思考内容可回传，chatReasoningBackfillKey 此时直接返回空；档位为 off 还照补就与档位语义自相矛盾）。Anthropic signature / Responses encrypted_content 是轮内载荷，只存 reasoningStash 不落盘。另：自定义 http.RoundTripper 消费 req.Body 后必须重设 Body+GetBody，否则 SDK 重试报 "ContentLength=X with Body length 0"。危害：重启/切回旧会话后首条请求 400；重试路径全部静默失败。@internal/app/prov_reasoning.go @internal/app/prov_model.go
[stream-final-value] 2026-09-12 小心：流式"终值事件"（Responses function_call_arguments.done / Anthropic message_delta.usage）携带的是累计终值，字段缺失时 SDK 解码成零值，直接赋值会清掉已累积状态；存在性必须用 SDK 的 respjson.Field.Valid() 判（Go 零值分不清“0”与“缺字段”），语义是“存在即覆盖、绝不累加”。危害：工具参数被清空→静默以 {} 执行；usage 缓存计数丢失。同类形状：Chat 路径 mergeToolCallDeltas 的“非空才合并”。@internal/app/prov_model.go
[stream-ease-split] 2026-09-14 小心：流式正文不能把“已接收长度”当“已显示长度”，否则上游 64ms 一批的字符就是十几帧一次的整块跳字；修法是显示层缓动（advanceShown 逐帧追赶）+ 按块边界切成 committed/tail 两段渲染（tail 逐帧、长尾降频），单帧成本才有界。另：整篇 v-html 全量替换会重建节点，任何“针对新增字符”的过渡动画都不可能生效。危害：输出“一下一下”生硬，且长回答每帧全量重解析把主线程打满。@frontend/src/components/StreamingMarkdownBody.vue @frontend/src/utils/streamEase.mjs
[markdown-body-child-margin] 2026-09-14 小心：把 .markdown-body 的内容拆进多个容器后，`.markdown-body > :first-child/:last-child` 的清零规则会落到容器上而不是内容块上。修法：在容器上补一层 `:first-child/:last-child > :first-child/:last-child` 守卫（只在消息首尾清零）。危害：要么消息首尾多出外边距，要么接缝处把段落间距吃掉、相邻段落挤成一坨。@frontend/src/style.css
[frontend-no-gate] 2026-09-14 小心：前端没有 TS/lint 门禁，`<script setup>` 里用了未 import 的标识符 `vite build` 不会报错（rollup 当全局变量处理），只在运行时抛 ReferenceError。危害：构建绿的但界面一点就白屏/报错；改完前端必须真跑一次界面再交付。@frontend/package.json
[effort-off-level] 2026-09-15 小心：“关闭思考”是独立档位，不能归一到 auto，而且**同一档位在不同协议上拼写不同**，映射必须收口在一处（reasoningWireForAdapter → reasoningWirePlan；Anthropic 在 configureAnthropicThinking）：Chat 兼容端点发 `thinking:{"type":"disabled"}`（DeepSeek 文档写法，官方 SDK 只能走 extra_body，所以靠 chatRequestRewriteTransport/patchChatRequestFields 顶层注入），Responses 发 `reasoning.effort:"none"`（DeepSeek 文档明确 none=关闭思考模式），官方 OpenAI Chat 端点只能发自己的枚举 `reasoning_effort:"none"`（它拒未知字段），Anthropic 发 `thinking.type:"disabled"`。注意“思考模式不支持 temperature/presence_penalty/frequency_penalty”（DeepSeek 文档：传了不报错但不生效），以及带了 tools 的请求必须完整回传 reasoning_content（参见 reasoning-replay 那条）。危害：折成 auto 会让“关闭思考”静默失效；每个适配器各自拍一个拼写则三方漂移。待办：无。别名表两侧已对齐并由两侧测试各自钉住（infra_bridges_test.go TestNormalizeReasoningEffort / modelConfigIO.test.mjs）：前端 normalizeReasoningEffort 曾缺 nothinking/nothink，手改的 "off" 会在配置加载时被折成 auto 并随任意一次保存落盘，等于思考被静默打开；容错表不一致比不容错更危险。另：normalizeApiFormat 已收口到 utils/modelConfigIO.mjs，别再在组件里就地写一份枚举归一。@internal/app/infra_bridges.go @internal/app/prov_reasoning.go @internal/app/prov_model.go @frontend/src/utils/modelConfigIO.mjs
[prompt-history-localstorage] 2026-09-15 小心：输入框召回历史这类本地文本缓存只有两种合法形态——要么结构性有界（每桶条数上限 + 单条长度上限 + 全库字符预算 + 工作区移除时显式弃桶，收口在 utils/promptHistoryStore.mjs），要么就明说重启即丢；只做一半必出事。危害：只加上限不做弃桶=配额慢性泄露（486cec7 曾因此整块改纯内存，结果就是用户报“重启后输入历史/草稿丢了”）。@frontend/src/utils/promptHistoryStore.mjs @frontend/src/App.vue
[tri-state-undefined-key] 2026-09-15 小心：前端的模型快照类对象（modelSnapshotFrom / applyCatalogPreset）必须**显式**带上三态字段（如 visionCapable: undefined 表示未知），不能“未知就不写这个键”。危害：ModelsPanel 用 Object.assign(modelDraft, preset) 套用目录预设，键缺失会保留上一个模型的能力值（静默继承 → 视觉模型切到纯文本模型后仍按能看图处理），只有显式 undefined 才清得掉；后端 *bool + omitempty 的 nil 语义与它对齐，别把 undefined 当成“实现脏”。@frontend/src/utils/modelProviderCatalog.mjs @frontend/src/App.vue
[internal-call-effort] 2026-09-15 小心：内部 LLM 调用（压缩总结、连通性测试等）不得改写用户的思考档位：压缩曾写死 `compactionCfg.ReasoningEffort = "low"`，与设置静默漂移；而换成“关闭思考”会让必须思考的模型直接 400。危害：用户设 max 实际按 low 跑、内部调用与 UI 显示不一致；修法：原样传 cfg（app.go compactHistory）。@internal/app/app.go
[protected-path-single-source] 2026-09-15 小心：受保护路径（VCS 元数据 .git/.svn/.hg、KB sources/）的判定必须一处收口，并由写（create/edit/编辑器保存/saveTo/move 目标）、删、命令目标三条入口共用；且路径比较前必须先归一：Windows 下 Win32 会剥掉每段尾部的点与空格而 filepath.Clean 不剥，`.git.` 就是真正的 `.git`。危害：只给 delete 加保护而 create/edit/`>` 重定向没有 → 往 .git/hooks 写一个文件，下次 git 提交即执行代码；`.git.` 绕过名字比较直接删掉仓库元数据。修法：pathutil.CanonicalPath + VCSMetadataReason（resolveWritableFilePath / resolveDeletablePath / firstVCSMetadataMutationTarget 共用）。另：mv/rename 会移除源，源操作数必须一并算变更目标（否则 `mv sources/x .`、`mv .git /tmp/x` 直接绕过只读与删除围栏）。@internal/tools/pathutil/pathutil.go @internal/app/orch_file_ops.go @internal/app/orch_command_safety.go @internal/tools/command/semantic.go
[lock-defer-with-recover] 2026-09-15 小心：函数内带 recover 时，互斥锁必须 defer 释放，禁止手工 Lock/Unlock 配对：recover 正好在“锁已持有、Unlock 尚未执行”时生效，锁永久不释放（AGENTS.md §4.1 的同一形状）。危害：一次工具 handler panic 从“单个工具失败”升级成进程级冻结——后续所有文件工具、编辑器保存、UI 绑定阻塞，run 永不结束、UI 永久 running、取消无效，只能重启。@internal/app/app.go
[scope-key-single-source] 2026-09-15 小心：按会话派生缓存键（reasoningStash 等）时，写入侧与清理侧的键必须由同一个函数派生；写入用 hash(sessionID)、清理却传裸 sessionID 会让清理永久空转，而“测试里把键写成裸值”正好掩盖它（测试与生产用了两套派生）。危害：回溯/释放会话后旧签名与加密 reasoning 继续被跨回合重放（可致 400），死条目还挤占 LRU、把在飞工具回合的载荷顶掉。@internal/app/prov_reasoning.go
[typed-4xx-keyword-split] 2026-09-15 小心：错误分类中 typed 状态码分支一旦 return，就必须在该分支内把关键词细分做完（注释以为后面的关键词阶段会补，实际走不到）。危害：`400 + maximum context length` 永远归不到 ContextTooLong，依赖该分类的恢复路径（上下文溢出强制压缩）与策略静默失效。@internal/app/prov_model.go
[read-size-guard-before-read] 2026-09-15 小心：读文件工具必须先 stat/限长再读，os.ReadFile 没有上限；“先整读再判断太大”等于给一次普通 read 调用留了 OOM 入口（伪造几步文件头即可）。危害：内存爆掉、进程被系统杀掉，未落盘的运行态全丢。修法：超大文件只 stat + 流式哈希，不 buffer。@internal/app/orch_read.go @internal/tools/read/read.go
[css-token-order-shadow] 2026-09-17 小心：style.css 里同类 token 覆盖块靠**源码顺序**决胜（`:root[data-mode="light"]` 与 `:root[data-theme="x"]` 同为 0,2,0 优先级），后写的整段吃掉先写的。修法（已落地）：把“模式”写进选择器里，让两轴互斥且都比基块更具体——深色变体 `:root[data-theme="x"]:not([data-mode="light"])`，浅色变体 `:root[data-theme="x"][data-mode="light"]`（后者 0,3,0 > 基块 0,2,0，且前者在浅色下根本不匹配）。危害：accent 主题的 6×14 行 markdown 调色被后加的亮色块静默覆盖成死代码，两个半月无人发现（同一套配色只在暗色下有效），同时 setter/UI 早在 d4906c6 被删，整条 `data-theme` 链已不可达却仍在维护。@frontend/src/style.css @frontend/src/utils/theme.mjs
[md-tokens-live-on-markdown-body] 2026-09-19 小心：中性 markdown token（`--md-text/--md-muted/--md-faint/--md-white/--md-border/--md-surface`）是声明在 `.markdown-body` **自身**上的，不是 :root——在 :root 里覆盖它们对正文无效（自己的声明胜过继承），必须在 `.markdown-body` 上写直接规则（亮色块与每个主题变体都各有一条）。只有 accent 系的 14 个（`--md-h1…--md-marker-ol`）是 :root 级。危害：新主题标题换了色、正文却还是上一套的冷灰/琥珀，看着像串色且极难定位。@frontend/src/style.css
[naive-popover-hosts-tooltip] 2026-09-19 小心：Naive 的 tooltip 和 popover 是**同一个元素**（`.n-popover.n-popover-shared`，PopoverBody 同时挂两个 class），而浅色模式下 tooltip 气泡是“黑底白字”的反色气泡（颜色来自 Tooltip 主题：`color: composite(baseColor, rgba(0,0,0,.85))` + `textColor: baseColor`，**不吃** popoverColor，只有 dark 主题才用 popoverColor）。危害：任何给 `.n-popover` / `.n-popover-shared` 写 background 的规则（`html[data-mode="light"] .n-popover` 或 `:root .n-popover`）都会连 tooltip 一起刷成浅色，白字直接看不见（左侧菜单悬浮名就是这么消失的）。修法：弹层只绑 `:root .n-modal`，popover/tooltip 一律交给 Naive 自己上色；需要区分时用 Naive 自带的 class 标记（tooltip 气泡带 `.n-tooltip`）。@frontend/src/style.css
[naive-theme-override-no-var] 2026-09-19 小心：不要把 `var(--x)` 传进 Naive `themeOverrides` 的 cardColor / modalColor / popoverColor，也不要用 `.n-card` 之类的反选去“让主题接管弹层”：Naive 内部会对这几个色做 composite() 数学运算（avatar / calendar / data-table / tooltip 都这么干），传 var() 会得到非法颜色；而 CSS 覆盖又会和组件内联的 `--n-color` 抢优先级。弹层表面要么留在 App.vue 的每模式 literals，要么只在 CSS 侧绑具体组件类（`.n-modal`）。另：`n-card` 并非“没渲染过”——本仓库的 card 全部来自 `preset="card"` 弹窗，而 Naive 把 NCard 渲染成 modal 元素**本身**（同一节点上同时带 `n-card` 与 `n-modal`，见 BodyWrapper），所以给 `.n-card` 写规则 = 与 `.n-modal` 重复声明（不致命，但是白写一条）。@frontend/src/App.vue @frontend/src/style.css
[light-scope-literal-leak] 2026-09-19 小心：只带 `data-mode` 不带主题轴的选择器（`html[data-mode="light"] .foo`）会命中**所有主题**的浅色；里面的瑰珀调字面量/通用 token 会原样漏进每个新主题。已收口（2026-09-19 换主题批次）：瑰珀专属浅色字面量一律带 `:not([data-theme])`（瑰珀=默认主题、无 data-theme 属性），其余主题走通用 token 规则；左侧菜单选中图标、tab 下划线、prompt-input 边框、session-time、mermaid-action 都已按此两档制改造。因此新增主题 = theme.mjs 一条 + style.css 深/浅两个块，**不再维护 :is() 主题名单**。危害（历史）：非瑰珀浅色主题菜单选中/tab 下划线一直是瑰珀调的绿。@frontend/src/style.css @frontend/src/components/AppHeader.vue @frontend/src/utils/theme.mjs
[link-affordance-not-status-token] 2026-09-19 小心：可点击/链接类 affordance（欢迎表格三个 popover 触发钮等）不要用全局状态色 token（`--ally-info-*` 只在基础 :root 与琥珀浅色块定义、主题不变，同 danger/warning 一样是“跨主题恒定”的语义）；链接的颜色归 theme accent（`--ally-accent-bright`，每主题深/浅块必有、与 --md-link 同源），否则每个主题里都是同一支固定蓝。危害：换主题后首页表格可点部分仍是琥珀调蓝，看着像 bug。@frontend/src/components/ToolsPopover.vue @frontend/src/components/McpStatusPopover.vue @frontend/src/components/SkillsPopover.vue
[picker-side-effect-adopts-active-session] 2026-09-21 小心：选目录类函数（chooseWorkspace 等）不得无条件把选中路径写进当前活动会话/config ——“为别处选目录”（新建 Tab、加 extraRoot）和“为当前 Tab 首次采纳工作区”是两种语义，副作用必须由调用方显式声明（adopt 参数）。危害：点 “+” 新建工作区 Tab 时旧 Tab 的活动会话被隐性改挂到新工作区：/sessions 列表看到别的工作区的会话并随后持久化到索引（重启仍在），同时它从原工作区列表里消失，全程无任何提示。@frontend/src/App.vue
[provider-cache-explicit-mode-is-off] 2026-09-21 小心：OpenAI Responses 的 `prompt_cache_options.mode="explicit"` 不是“显式缓存”，而是**关掉**隐式断点；请求里若没有自己的 `prompt_cache_breakpoint`，等于完全不缓存（SDK 文档原话如此，pi 也只在 CacheRetention="none" 时才选它）。危害：gpt-5.6 长会话的正文（占输入 90%+）每轮按全价重算，命中率上限只剩 header 占比。修法：默认走隐式模式（不写 mode、不在 input 里塞锚点），延长缓存用 prompt_cache_retention（GPT-5.6 之前）/ prompt_cache_options.ttl（GPT-5.6+），拼写收口在 prov_wire_config.go。@internal/app/prov_model.go @internal/app/prov_wire_config.go
[cache-route-vs-replay-scope] 2026-09-21 小心：子代理的“缓存路由键”与“思考回放台账 scope”是两种身份，不得共用一个字段：路由键要按 lane 稳定（同一会话的多次委派复用同一份 header 缓存，pi 用 `<session id>:<lane name>`），台账 scope 必须按 run 隔离（回放按首个 tool_call id 匹配并**替换**，中转把每条响应都重编成 call_0 时两个并发 run 会互相顶掉签名）。危害：共用 per-run key → 每次子代理冷启动；共用稳定 key → 并发子代理互串签名；且 per-run key 无人清理会占满台账 LRU。修法：ConfigState.reasoningScope 与 responsesPromptCacheKey 分开，run 结束 clearScope。@internal/app/orch_subagent.go @internal/app/prov_reasoning.go
[dedup-hash-must-cover-full-payload] 2026-09-21 小心：做“内容没变就不再重复发送”的去重时，哈希必须覆盖模型实际收到的**全部载荷**，而不是其中那段可读文本。读图片的结果里图片本体在 dataURL、正文只有一句“文件名+体积”的说明——只哈希说明文字时，同名同体积的换图会被当成“没变”。危害：用户换了截图，模型仍只拿到旧说明且被告知“内容已给过”，新图永远进不了上下文。@internal/app/orch_read.go
[retry-after-cap] 2026-09-22 小心：重试退避必须与服务端 Retry-After 取大者且设上限（60s）兑底；sashabaranov 错误不带响应头，Chat 路径须经传输层捕获（retryAfterCaptureTransport）再附到错误上。@internal/app/prov_model.go @internal/app/prov_proxy.go
[unsigned-thinking-endpoint] 2026-09-22 小心：无签名 thinking 块的回放判据是端点不是模型名：官方端点丢弃（校验签名）、兼容网关补空串照常回放（只看字段存在性）。@internal/app/prov_reasoning.go
[compact-single-path] 2026-09-22 小心：所有压缩入口（手动/阈值/溢出）必须走同一条两级路径——先微压缩，仍超阈值才 LLM 总结；禁止给任何入口开特例（手动曾无条件总结致漂移）。@internal/app/biz_compact.go
[contain-changes-positioning-context] 2026-09-22 小心：`content-visibility: auto`（含隐含的 `contain: layout paint`）会让元素成为绝对定位后代的包含块，并把它自己的盒子当作裁剪边界——加它之前必须确认两件事：该元素**本来就是**定位祖先，且没有故意溢出自身盒子的子元素。本项目五处都先确认过：`.message` 自身已是 `position: relative`（ChatMessages.vue 样式块），删除按钮 `right: 8px` 与悬停统计行都在行内；`.rich-tool-card` 则早先用 `margin-left: -20px` + `padding-left: 20px` 把悬挂状态图标纳入自身盒内。危害：给原本不是定位祖先的元素加 containment，会把原本相对外层定位的角标/浮层静默钉到该行，视觉错位且极难定位来源；有溢出子元素的则直接裁掉。另：`contain-intrinsic-size: auto <len>` 里的 `auto` 只对“已被量过一次”的元素生效，首次出现仍用 `<len>`，所以估计值不能太离谱（本项目取值：工具卡/diff 行 42/20px，mermaid 卡 320px，消息行 120px，会话行 42px）。@frontend/src/style.css @frontend/src/components/ChatMessages.vue
[terminal-no-hidewindow] 2026-09-22 起终端（wt.exe / start cmd.exe）绝不加 SysProcAttr.HideWindow：SW_HIDE 会顺着激活链传染给真正被创建出来的终端窗口（实测 `wt.exe -w new` 的窗口 IsWindowVisible=False，用户只看到“点了没反应”）；要“不闪黑框”用 CREATE_NO_WINDOW。另：`wt.exe -d` 是把标签页交给最近使用的那个窗口，那个窗口最小化时不会被带回来（实测始终 minimized），要 -w new 才保证可见。@internal/app/host_terminal_windows.go
[fixed-static-position-offscreen] 2026-09-22 小心：`position: fixed` 又不写 left/top 的元素是按“静态位置”落位的，不是视口左上角；本项目浮层一律 teleport 到 body、排在占满整屏的 #app 之后 → 基准纵坐标=视口高度，只用 transform 定位的卡片整块掉到窗口下方完全看不见（实测：视口高 705、translate y=200 → 测得 top=905；补上 left:0/top:0 后 =200）。修法：用 transform 定位的 fixed 浮层必须同时钉 left/top（transform 依旧走合成，不损失性能）。@frontend/src/components/AppHeader.vue @frontend/src/components/DigitalWave.vue
